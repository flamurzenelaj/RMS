let fs;_4f0‍.x([["default",()=>_4f0‍.o]]);_4f0‍.w("fs",[["default",["fs"],function(v){fs=v}]]);
const filesDir = `${__dirname}/../public/images/`;
const FILE_SEPERATOR = ";";

const FileService = {   
    uploadFiles: (files) => {
        if (!fs.existsSync(filesDir)) {
            fs.mkdirSync(filesDir);
        }

        const filenames = files.map((file) => {
            const filename = file.name;
            fs.writeFileSync(filesDir + filename,file.data);
            return filename;
        }).join(FILE_SEPERATOR);
        return filenames;
    },

    deleteFiles: (filenames) =>
        filenames.map((filename) => fs.unlinkSync(filesDir + filename))
};
_4f0‍.d(FileService);